{
  "name": "sys-monitor-api",
  "version": "1.0.0",
  "description": "A lightweight API that exposes the system's current performance (such as disk, network, cpu/temperature etc)",
  "main": "server.js",
  "scripts": {
    "start": "forever server.js"
  },
  "keywords": [
    "system",
    "monitor",
    "http",
    "api"
  ],
  "author": "David Nedved",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "forever": "^0.14.2",
    "node-os-utils": "^1.3.7",
    "remove-trailing-spaces": "^1.0.8",
    "sequence": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
